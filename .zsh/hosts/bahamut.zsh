# Machine-specific zsh config for bahamut (Mac mini -- home / primary)
# Tracked in git. NON-SECRET config only -- secrets go in ~/.zshrc.local

# Named directory shortcut for the Dock NVMe SSD (use ~dock)
hash -d dock=/Volumes/Dock
